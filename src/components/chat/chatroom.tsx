import { Card } from "@/components/ui/card";
import { IChatroom } from "@/services/types";

interface ChatroomProps {
  chatroom: IChatroom;
}

export const Chatroom = ({ chatroom }: ChatroomProps) => {
  return <Card className="w-full">
    
  </Card>;
};